I want to safely implement three production scaling enhancements across our full-stack monorepo workspace. Please follow these precise instructions step-by-step. Do not attempt all steps simultaneously; implement, build, and verify each step before moving to the next.

---

### STEP 1: Database Connection Pooling
1. Locate our database client initialization file.
2. Refactor it to pass a robustly configured `pg.Pool` instance into Drizzle (`drizzle(pool, { schema })`) instead of using a single direct client connection. 
3. Configure the pool with a maximum connection cap (`max: process.env.NODE_ENV === 'production' ? 20 : 5`), an idle timeout of 30 seconds, and a connection timeout of 2 seconds.
4. Ensure that NO existing database queries, schemas, or `db.select()` / `db.insert()` calls across the rest of the app are modified. They should consume the updated pooled export seamlessly.

---

### STEP 2: Hardening esbuild Configurations (CJS/ESM Interop)
1. Review our `esbuild` configuration script (used to bundle the server code).
2. Ensure that backend-heavy dependencies mentioned in our gotchas—specifically `stripe`, `pdf-parse`, `@clerk/backend`, and `pg`—are explicitly added to the `external` array configuration. 
3. This is a purely defensive measure to ensure that runtime paths like `__dirname` are preserved when compiling into CommonJS (CJS). Do not modify the target format from CJS.

---

### STEP 3: Add Additive Local Tenant Context Middleware
1. Create a new middleware file at `artifacts/api-server/src/middlewares/tenantContext.ts`.
2. Do NOT replace or alter our existing Clerk authentication wrapper; this new middleware must sit right after it as an additive layer.
3. Write an `injectTenantContext` middleware that reads the `req.auth?.userId` provided by Clerk.
4. Use Drizzle to query our local PostgreSQL `users` table to fetch that user's `active_company_id` and role.
5. Bind this data directly to the incoming request object as `req.tenantContext = { userId, companyId, role }`.
6. For safety during this rollout, do not enforce this globally yet. Apply it to a single test or new API route so we can verify the context injection works perfectly without affecting the main web app dashboard or mobile endpoints.

---

### VERIFICATION & COMPLIANCE RULES:
- **No Contract Breakages:** Do not modify `lib/api-spec/openapi.yaml` or any auto-generated files under `lib/api-zod/` or `lib/api-client-react/`.
- **Workspace Quality:** After executing each step, run `pnpm run typecheck` and `pnpm run build` to guarantee that TypeScript remains 100% clean and zero compiler regressions are introduced.
- Inform me immediately when Step 1 is complete and verified so we can test the dashboard connection before proceeding.