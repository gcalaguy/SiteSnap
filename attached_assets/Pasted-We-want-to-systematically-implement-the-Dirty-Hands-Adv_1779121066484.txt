We want to systematically implement the "Dirty Hands" Advantage across the entire mobile application layout (`artifacts/mobile/app`). The goal is to eliminate deep menu navigation, typing, and heavy tapping for field foremen, owner, and workers, who have gloved or dirty hands, allowing them to control the app entirely via voice while maintaining a single global microphone interaction.

Please execute the following implementation safely without breaking existing component layouts:

### 1. Upgrade the Voice Router for Compound Intents
Locate our isolated utility file `artifacts/mobile/app/utils/voiceRouter.ts` and expand its `interpretVoiceCommand` regex/parsing logic to handle **Compound Intents** (multi-action commands spoken in a single sentence).
- It must identify and split complex phrases containing both a timesheet action and a daily log action. 
- Example input: "Log 4 hours for Guy on the 123 Basement project and note that the sub-grade is wet"
- Expected output object must cleanly isolate both payloads:
  {
    intent: "COMPOUND_ACTION",
    actions: [
      { type: "LOG_HOURS", worker: "Guy", hours: 4, project: "123 Basement" },
      { type: "ADD_DAILY_LOG", project: "123 Basement", notes: "sub-grade is wet" }
    ]
  }

### 2. Map the "Dirty Hands" Universal Vocab Matrix
Expand the pattern matching in `voiceRouter.ts` to capture these high-frequency, tool-in-hand field actions:
- **Material Alerts:** "We are short on 2x4 studs" -> { type: "MATERIAL_ALERT", item: "2x4 studs" }
- **Photo Captures:** "Take a photo of the foundation crack" -> { type: "TRIGGER_CAMERA", context: "foundation crack" }
- **Safety Flags:** "Subcontractor missing PPE at 123 Basement" -> { type: "SAFETY_LOG", project: "123 Basement", issue: "missing PPE" }

### 3. Ensure Safe State Execution
- Ensure that if a `COMPOUND_ACTION` is returned, the app handles the execution sequentially without locking the UI thread.
- If an intent lacks a project name but the user is currently looking at a specific project screen (like the 123 Basement detail page), default the payload context to that active project automatically rather than throwing an error or prompting for re-typing.

Provide a code summary showing the updated `VoiceIntent` type definitions and how the string-splitting logic isolates compound parameters before committing.