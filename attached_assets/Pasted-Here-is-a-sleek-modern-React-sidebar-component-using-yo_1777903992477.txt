Here is a sleek, modern React sidebar component using your new Black, Gold, and Bright White palette.

I’ve used primary for the background, secondary for the accents/logo, and foreground for that crisp white text you requested.

TypeScript
import React from 'react';
import { Home, Layout, Settings, PieChart, Box } from 'lucide-react';

const Sidebar = () => {
  const menuItems = [
    { icon: <Home size={20} />, label: 'Dashboard', active: true },
    { icon: <Box size={20} />, label: 'Assets', active: false },
    { icon: <PieChart size={20} />, label: 'Analytics', active: false },
    { icon: <Settings size={20} />, label: 'Settings', active: false },
  ];

  return (
    <div className="h-screen w-64 bg-primary flex flex-col border-r border-secondary/20">
      {/* Logo Section */}
      <div className="p-6 flex items-center gap-3">
        <div className="w-8 h-8 bg-secondary rounded-md flex items-center justify-center">
          <Layout className="text-primary" size={20} />
        </div>
        <span className="text-foreground font-bold text-xl tracking-tight">
          ASSET<span className="text-secondary">MG</span>
        </span>
      </div>

      {/* Navigation */}
      <nav className="flex-1 px-4 py-4 space-y-2">
        {menuItems.map((item, index) => (
          <div
            key={index}
            className={`flex items-center gap-4 px-4 py-3 rounded-lg cursor-pointer transition-all duration-200
              ${item.active 
                ? 'bg-secondary text-primary font-semibold' 
                : 'text-foreground hover:bg-secondary/10 hover:text-secondary'}`}
          >
            {item.icon}
            <span>{item.label}</span>
          </div>
        ))}
      </nav>

      {/* User Profile Area */}
      <div className="p-4 border-t border-secondary/10">
        <div className="flex items-center gap-3 p-2">
          <div className="w-10 h-10 rounded-full bg-secondary/20 border border-secondary" />
          <div className="overflow-hidden">
            <p className="text-foreground text-sm font-medium truncate">Admin User</p>
            <p className="text-secondary text-xs truncate italic">Premium Account</p>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Sidebar;